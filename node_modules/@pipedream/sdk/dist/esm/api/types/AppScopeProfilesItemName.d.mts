/** The profile name. Pass this back as `oauth_scope_profile` when starting the OAuth flow for this app. */
export declare const AppScopeProfilesItemName: {
    readonly ReadOnly: "read_only";
    readonly ReadWrite: "read_write";
    readonly Admin: "admin";
};
export type AppScopeProfilesItemName = (typeof AppScopeProfilesItemName)[keyof typeof AppScopeProfilesItemName];
