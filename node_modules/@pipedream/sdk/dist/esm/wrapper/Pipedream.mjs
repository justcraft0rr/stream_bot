var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
import { ProjectEnvironment } from "../api/index.mjs";
import { WorkflowsClient } from "../api/resources/workflows/client/Client.mjs";
import { PipedreamClient } from "../Client.mjs";
import { PipedreamEnvironment } from "../environments.mjs";
export class Pipedream extends PipedreamClient {
    constructor(opts = {}) {
        var _a, _b, _c;
        const { baseUrl = (_a = process.env.PIPEDREAM_BASE_URL) !== null && _a !== void 0 ? _a : PipedreamEnvironment.Prod, headers, projectEnvironment = process.env.PIPEDREAM_PROJECT_ENVIRONMENT, projectId = (_b = process.env.PIPEDREAM_PROJECT_ID) !== null && _b !== void 0 ? _b : "", workflowDomain = process.env.PIPEDREAM_WORKFLOW_DOMAIN, } = opts || {};
        if (projectEnvironment != null &&
            projectEnvironment !== ProjectEnvironment.Production &&
            projectEnvironment !== ProjectEnvironment.Development) {
            throw new Error(`Project environment must be either '${ProjectEnvironment.Production}' or '${ProjectEnvironment.Development}'`);
        }
        const clientOpts = {
            baseUrl,
            headers,
            projectEnvironment,
            projectId,
        };
        if ("tokenProvider" in opts) {
            clientOpts.tokenProvider = opts.tokenProvider;
        }
        else {
            const { clientId = process.env.PIPEDREAM_CLIENT_ID, clientSecret = process.env.PIPEDREAM_CLIENT_SECRET } = opts || {};
            if (!clientId || !clientSecret) {
                throw new Error("Client ID and secret are both required and cannot be blank");
            }
            if (!projectId) {
                // Project ID is required here because it cannot be inferred
                // from the client ID/secret, as opposed to the case with access
                // tokens.
                throw new Error("Project ID cannot be blank");
            }
            // Default to production for client ID/secret auth
            (_c = clientOpts.projectEnvironment) !== null && _c !== void 0 ? _c : (clientOpts.projectEnvironment = ProjectEnvironment.Production);
            clientOpts.clientId = clientId;
            clientOpts.clientSecret = clientSecret;
        }
        super(clientOpts);
        this._workflowDomain = workflowDomain;
    }
    /**
     * Returns an access token that can be used to authenticate API requests
     *
     * @returns A promise that resolves to the access token string.
     */
    get rawAccessToken() {
        return this._tokenProvider.getToken();
    }
    get workflows() {
        var _a;
        return ((_a = this._workflows) !== null && _a !== void 0 ? _a : (this._workflows = new WorkflowsClient(Object.assign(Object.assign({}, this._options), { token: () => __awaiter(this, void 0, void 0, function* () { return yield this._tokenProvider.getToken(); }), workflowDomain: this._workflowDomain }))));
    }
}
