/**
 * A webhook object including the signing key
 */
export interface WebhookWithSigningKey {
    /** The unique ID of the webhook */
    id: string;
    /** The webhook URL */
    url: string;
    /** The webhook signing key */
    signingKey: string;
    /** Whether a signing key has been set for this webhook */
    signingKeySet: boolean;
    /** The time the webhook was created, in epoch seconds */
    createdAt: number;
    /** The time the webhook was last updated, in epoch seconds */
    updatedAt: number;
}
