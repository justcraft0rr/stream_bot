/**
 * A trigger webhook object. `signing_key` is only returned for OAuth-authenticated requests.
 */
export interface TriggerWebhook {
    /** The unique ID of the webhook */
    id: string;
    /** The webhook URL */
    url: string;
    /** The webhook signing key, returned only for OAuth-authenticated requests */
    signingKey?: string;
    /** Whether a signing key has been set for this webhook */
    signingKeySet: boolean;
}
